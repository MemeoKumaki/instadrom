<%= form_for(@comment) do |f| %>
  <%= f.hidden_field :user_id, :value => current_user.id %>
  <%= f.hidden_field :post_id, :value => p.id %>
  <%= f.text_area :body %>
  <%= f.submit 'Comment' %>
<% end %>